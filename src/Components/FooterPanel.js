import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const footer = () => {
	return (
		<Footer>
			<p>Data to be populated soon.</p>
		</Footer>
	);
};

export default footer;
